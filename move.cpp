#include "Move.h"
#include <stdio.h>
void Move::keyboardinput(int k)
{
	if (k == 'a')
	{
		printf("\na ok");
	}

	if (k == 'd')
	{
		printf("\nd ok");
	}

	if (k == 'w')
	{
		printf("\nw ok");
	}
}